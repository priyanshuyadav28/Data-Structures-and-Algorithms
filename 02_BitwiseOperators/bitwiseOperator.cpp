#include<iostream>
using namespace std;

int main() {

    // bool a = false;
    // bool b = false;

    // // cout << (a && b) << endl;
    // cout << ~a; 

    // left shift operator 
    // int a = 12;
    // a = a << 1;

    // cout << a << endl; // by performing left shift operator on a number by 1 the number gets doubled or it is multiplied by 2 each time it is shifted. 

    int b = 12;
    b = b >> 1;

    cout << b; // by performing right shift operator on a number the number gets divided by 2 or gets halfed . but there is a catch in it find it . 
    

    return 0;
}